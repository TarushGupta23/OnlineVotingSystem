import Navbar from '../../components/Navbar/Navbar'
import './landingPage.css'

export default function LandingPage() {
    return <div id='landing-page'>
        <Navbar />
        <section>
        {/* btns for:  */}
            {/* create vote */}
            {/* cast vote */}
            {/* vote someone */}
        </section>
        <section>
            {/* title + filter and search */}

            {/* your votes - where u already can vote */}
            {/* vote for - open to public votes */}
            {/* vote for - need to apply to vote */}
        </section>
        <section>
            {/* title + filter and search */}
            {/* votes you participated in and their results */}
        </section>
    </div>
}